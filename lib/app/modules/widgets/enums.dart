enum Voting {
  up,
  down,
}

enum Vaccination {
  firstDose,
  secondDose,
}

enum FormButton {
  personal,
  academic,
  medical,
}
enum UserType {
  student,
  faculty,
}

enum ifLiked {
  blue,
  grey,
}
enum ifDisliked {
  red,
  grey,
}
